{
  "private": true,
  "scripts": {
    "test": "percy exec --config .percy.yml -- playwright test --reporter=dot",
    "test1": "playwright test --headed"
  },
  "dependencies": {
    "todomvc-app-css": "^2.3.0"
  },
  "devDependencies": {
    "@percy/cli": "1.11.0",
    "@percy/playwright": "^1.0.4",
    "@playwright/test": "^1.20.0",
    "playwright": "^1.20.0",
    "serve-handler": "^6.1.3",
    "scroll-to-bottomjs": "1.1.0"
  }
}
